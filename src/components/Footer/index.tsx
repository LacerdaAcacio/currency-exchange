import { LABELS } from "../../constants";
import { FooterContainer } from "../../styles/styles";

const Footer = () => {
  return (
    <>
      <FooterContainer>{LABELS.FOOTER}</FooterContainer>
    </>
  );
};

export default Footer;
